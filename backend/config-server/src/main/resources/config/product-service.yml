server:
  port: 8082
spring:
  datasource:
    url: jdbc:h2:mem:smartcart_products;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
smartcart:
  seed-products: true
